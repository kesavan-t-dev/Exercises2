Remove the unnecessary bg-color for some fields, except submit button.
Keep the button name as 'Submit' not 'Submit Query'.
Remove the underline for Powered By and while hover the logo need to add tooltip.
Remove the border-radius for the input fields which applied.

additonal things:
- change the kebab-case to snake_cases
- add tooltip to top logo 
- change the number fields input length into 3,3,4.